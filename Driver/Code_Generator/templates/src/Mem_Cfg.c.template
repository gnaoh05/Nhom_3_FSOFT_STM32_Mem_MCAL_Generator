/* ========================================================================== */
/* AUTOSAR MEMORY DRIVER (MEM) - CONFIGURATION SOURCE                        */
/* Generated by MCAL Configurator Tool - DO NOT EDIT MANUALLY                 */
/* ========================================================================== */
#include "Mem_Cfg.h"

/* ========================================================================== */
/* 1. Sinh mảng cấu hình các Sector Batch của Flash STM32F401RE              */
/* ========================================================================== */
static const Mem_SectorBatchConfigType STM32F401RE_FlashBatches[{{ sector_batches | length }}] = {
{% for batch in sector_batches %}
    {
        /* {{ batch.comment }} */
        .MemStartAddress       = {{ batch.MemStartAddress }}u,
        .MemNumberOfSectors    = {{ batch.MemNumberOfSectors }}u,
        .MemEraseSectorSize    = {{ batch.MemEraseSectorSize }}u, /* {{ batch.size_comment }} */
        .MemMinReadSize        = {{ batch.MemMinReadSize }}u,
        .MemWritePageSize      = {{ batch.MemWritePageSize }}u,
        .MemSpecifiedEraseCycles = {{ batch.MemSpecifiedEraseCycles }}u
    }{{ "," if not loop.last }}
{% endfor %}
};

/* ========================================================================== */
/* 2. Sinh mảng Instance (Mem_InstanceConfigType)                             */
/* ========================================================================== */
static const Mem_InstanceConfigType Mem_Instances[MEM_MAX_INSTANCES] = {
    {
        .MemInstanceId      = 0u,
        .MemNumberOfBatches = {{ sector_batches | length }}u,
        .MemSectorBatches   = STM32F401RE_FlashBatches
    }
};

/* ========================================================================== */
/* 3. Đóng gói vào biến Root toàn cục [SWS_Mem_10000]                        */
/*    (Chính là biến extern bên file .h)                                      */
/* ========================================================================== */
const Mem_ConfigType Mem_ConfigData = {
    .MemInstances = Mem_Instances
};
